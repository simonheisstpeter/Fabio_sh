---
const { cat } = Astro.props;
---

<li>
    <button
        data-category={cat}
        class="filter-btn border border-divider text-muted2 hover:border-edge hover:text-fg px-3 py-1 rounded-full text-xs transition-colors"
    >
        {cat}
    </button>
</li>
