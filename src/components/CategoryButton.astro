---
const { cat } = Astro.props;
---

<li>
    <button
        data-category={cat}
        class="filter-btn border border-gray-300 text-gray-500 hover:border-gray-500 hover:text-gray-900 px-3 py-1 rounded-full text-xs transition-colors"
    >
        {cat}
    </button>
</li>
