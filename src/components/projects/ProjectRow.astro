---
import type { Project } from "../../lib/db";
import type { Translations } from "../../i18n/index";
import ProjectStatus from "./ProjectStatus.astro";
import ProjectCategories from "./ProjectCategories.astro";

interface Props {
    project: Project;
    locale: string;
    t: Translations;
}

const { project, locale, t }: Props = Astro.props;

const description =
    project.description[locale] ||
    project.description["en"] ||
    project.description["de"] ||
    "";

const year = new Date(project.createdAt).getFullYear();
const shownCategories = project.categories.slice(0, 2);
---

<li
    class="project-item"
    data-title={project.title.toLowerCase()}
    data-description={description.toLowerCase()}
    data-categories={JSON.stringify(project.categories)}
    data-created={project.createdAt}
>
    <div
        class="flex flex-wrap items-center gap-x-4 gap-y-1 border-b border-divider py-3"
    >
        <div class="min-w-0 flex-1">
            <ProjectStatus
                title={project.title}
                online={project.online}
                finished={project.finished}
            />
        </div>

        {
            shownCategories.length > 0 && (
                <ul class="m-0 flex shrink-0 flex-wrap gap-1 p-0">
                    {shownCategories.map((category) => (
                        <ProjectCategories category={category} />
                    ))}
                </ul>
            )
        }

        {
            project.url && (
                <a
                    href={project.url}
                    target="_blank"
                    rel="noopener noreferrer"
                    aria-label={t.website}
                    class="shrink-0 text-muted2 hover:text-emerald-400 transition-colors duration-200"
                >
                    <svg
                        xmlns="http://www.w3.org/2000/svg"
                        viewBox="0 0 16 16"
                        fill="currentColor"
                        class="h-3.5 w-3.5"
                        aria-hidden="true"
                    >
                        <path d="M6.22 8.72a.75.75 0 001.06 1.06l5.22-5.22v1.69a.75.75 0 001.5 0v-3.5a.75.75 0 00-.75-.75h-3.5a.75.75 0 000 1.5h1.69L6.22 8.72z" />
                        <path d="M3.5 6.75c0-.69.56-1.25 1.25-1.25H7A.75.75 0 007 4H4.75A2.75 2.75 0 002 6.75v4.5A2.75 2.75 0 004.75 14h4.5A2.75 2.75 0 0012 11.25V9a.75.75 0 00-1.5 0v2.25c0 .69-.56 1.25-1.25 1.25h-4.5c-.69 0-1.25-.56-1.25-1.25v-4.5z" />
                    </svg>
                </a>
            )
        }

        <span class="shrink-0 text-xs text-muted3 tabular-nums">{year}</span>
    </div>
</li>
